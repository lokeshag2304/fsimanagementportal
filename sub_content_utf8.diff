diff --git a/src/app/[role]/subscription/SubscriptionContent.tsx b/src/app/[role]/subscription/SubscriptionContent.tsx
index 5425f8b..09831ac 100644
--- a/src/app/[role]/subscription/SubscriptionContent.tsx
+++ b/src/app/[role]/subscription/SubscriptionContent.tsx
@@ -1,4 +1,5 @@
 "use client";
+import { formatDate } from "@/utils/dateFormatter";
 import React, { useState, useEffect, useRef, useMemo, useCallback } from "react";
 import { Header } from "@/components/layout";
 import { GlassCard, GlassButton } from "@/components/glass";
@@ -45,7 +46,6 @@ import { normalizeEntityPayload } from "@/utils/normalizePayload";
 import { emitEntityChange } from "@/lib/entityBus";
 import { formatLastUpdated } from "@/utils/dateFormatter";
 import { getCurrencySymbol, currencySymbols } from "@/utils/currencies";
-
 interface Subscription {
   remark_id?: number | null;
   id: number;
@@ -335,7 +335,7 @@ const SubscriptionRow = React.memo(({
                     value={(editData[item.id] as any)?.grace_end_date || ""}
                     onChange={(e) => handleEditChange(item.id, "grace_end_date" as any, e.target.value)}
                     min={editData[item.id]?.renewal_date || item.renewal_date || undefined}
-                    className="w-full px-2 py-1 bg-white/5 border border-blue-500/30 rounded text-white text-sm focus:outline-none focus:ring-1 focus:ring-blue-500/50 focus:border-blue-500/30 backdrop-blur-sm"
+                    className="w-full min-w-[150px] px-2 py-1 bg-white/5 border border-blue-500/30 rounded text-white text-sm focus:outline-none focus:ring-1 focus:ring-blue-500/50 focus:border-blue-500/30 backdrop-blur-sm"
                     style={{ minHeight: "32px" }}
                   />
                 </td>
@@ -381,7 +381,7 @@ const SubscriptionRow = React.memo(({
               </div>
             </td>
             <td className="py-3 px-4 text-sm text-gray-300">
-              {item.renewal_date ? new Date(item.renewal_date).toLocaleDateString() : "--"}
+              {item.renewal_date ? formatDate(item.renewal_date) : "--"}
             </td>
             <td className="py-3 px-4">
               <div className="inline-flex items-center whitespace-nowrap gap-1 px-3 py-1 rounded-full text-xs font-medium backdrop-blur-sm bg-gray-500/20 text-gray-400 border border-gray-500/20">
@@ -389,7 +389,7 @@ const SubscriptionRow = React.memo(({
               </div>
             </td>
             <td className="py-3 px-4 text-sm text-gray-300">
-              {item.deletion_date ? new Date(item.deletion_date).toLocaleDateString() : "--"}
+              {item.deletion_date ? formatDate(item.deletion_date) : "--"}
             </td>
             <td className="py-3 px-4 whitespace-nowrap">
               <span className="text-sm">
@@ -1234,26 +1234,7 @@ export default function SubscriptionsPage() {
     return d;
   };
 
-  const formatDate = (dateString: string | null | undefined) => {
-    if (!dateString || dateString === "--") return "--";
-
-    // If it already looks like our target format, preserve it
-    if (typeof dateString === 'string' && dateString.includes(',') && dateString.includes(':')) {
-      return dateString;
-    }
-
-    try {
-      const date = new Date(dateString);
-      if (isNaN(date.getTime())) return dateString;
-
-      return date.toLocaleDateString("en-GB", {
-        day: "numeric",
-        month: "numeric",
-        year: "numeric",
-      });
-    } catch {
-      return dateString;
-    }
+  
   };
 
   const calculateDays = (expiryDate: string | null | undefined) => {
@@ -1418,7 +1399,7 @@ export default function SubscriptionsPage() {
           {/* Table Container */}
           <div className="overflow-hidden rounded-xl border border-white/10 backdrop-blur-sm">
             <div className="max-h-[500px] overflow-y-auto overflow-x-auto scrollbar-thin scrollbar-thumb-gray-400/50 scrollbar-track-gray-800/50">
-              <table className={`w-full table-fixed ${isClient ? 'min-w-[1200px]' : 'min-w-[2400px]'}`}>
+              <table className={`w-full table-auto ${isClient ? 'min-w-[1200px]' : 'min-w-[2400px]'}`}>
                 <thead>
                   <tr className="bg-white/5 border-b border-white/10">
                     <th className="py-3 px-4 text-left w-[48px]">
@@ -1460,7 +1441,7 @@ export default function SubscriptionsPage() {
                     </th>
                     {!isClient && (
                       <>
-                        <th className="py-3 px-4 text-left text-sm font-medium text-gray-300 w-[140px]">
+                        <th className="py-3 px-4 text-left text-sm font-medium text-gray-300 min-w-[150px]">
                           Grace Period
                         </th>
                         <th className="py-3 px-4 text-left text-sm font-medium text-gray-300 w-[160px]">
@@ -1640,7 +1621,7 @@ export default function SubscriptionsPage() {
                                 value={(newRecordData as any).grace_end_date || ""}
                                 onChange={(e) => handleNewRecordChange("grace_end_date" as any, e.target.value)}
                                 min={newRecordData.renewal_date || undefined}
-                                className="w-full px-2 py-1 bg-white/5 border border-blue-500/30 rounded text-white text-sm focus:outline-none focus:ring-1 focus:ring-blue-500/50 focus:border-blue-500/30 backdrop-blur-sm"
+                                className="w-full min-w-[150px] px-2 py-1 bg-white/5 border border-blue-500/30 rounded text-white text-sm focus:outline-none focus:ring-1 focus:ring-blue-500/50 focus:border-blue-500/30 backdrop-blur-sm"
                                 style={{ minHeight: "32px" }}
                               />
                             </td>
